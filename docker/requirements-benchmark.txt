# Core ACE dependencies
# Note: ace-framework is installed from local source in Dockerfile

# HuggingFace datasets
datasets>=2.14.0
huggingface-hub>=0.17.0

# LLM providers
litellm>=1.0.0
openai>=1.0.0
anthropic>=0.18.0

# Observability (optional but recommended)
opik>=0.1.0

# Structured outputs
instructor>=1.0.0
pydantic>=2.0.0

# Benchmark-specific dependencies (optional - install as needed)
# appworld>=0.1.0      # For AppWorld benchmark
# swebench>=1.0.0      # For SWE-bench benchmark
# letta-evals>=0.1.0   # For Letta benchmark

# Docker SDK for container management
docker>=6.0.0

# Utilities
python-dotenv>=1.0.0
tenacity>=8.0.0
